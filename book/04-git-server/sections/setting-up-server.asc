[[_setting_up_server]]
=== সার্ভার সেটআপ করা

আসুন, সার্ভার সাইডে SSH access সেটআপ করার প্রক্রিয়াটা দেখে নিই।
এই উদাহরণে, আপনার ইউজারদের অথেনটিকেট করতে `authorized_keys` পদ্ধতি ব্যবহার করবেন।
এটাও ধরে নিচ্ছি, আপনি Ubuntu-র মতো একটা standard Linux distribution চালাচ্ছেন।

[NOTE]
====
এখানে যা বলা হয়েছে তার বেশ কিছুটা `ssh-copy-id` কমান্ড ব্যবহার করে automate করা যায়, পাবলিক key ম্যানুয়ালি কপি আর ইন্সটল করার বদলে।
====

প্রথমে, একটা `git` ইউজার অ্যাকাউন্ট আর সেই ইউজারের জন্য একটা `.ssh` ডিরেক্টরি তৈরি করুন।

[source,console]
----
$ sudo adduser git
$ su git
$ cd
$ mkdir .ssh && chmod 700 .ssh
$ touch .ssh/authorized_keys && chmod 600 .ssh/authorized_keys
----

এরপর, `git` ইউজারের `authorized_keys` ফাইলে কিছু ডেভেলপারের SSH public key যোগ করতে হবে।
ধরে নিচ্ছি, আপনার কাছে কিছু trusted public key আছে আর সেগুলো temporary ফাইলে সেভ করে রেখেছেন।
আবারও বলি, public key দেখতে এরকম কিছু একটা হয়:

[source,console]
----
$ cat /tmp/id_rsa.john.pub
ssh-rsa 
dAv8JggJICUvax2T9va5 gsg-keypair
----

আপনি এগুলো `git` ইউজারের `.ssh` ডিরেক্টরিতে থাকা `authorized_keys` ফাইলে শুধু append করে দিন:

[source,console]
----
$ cat /tmp/id_rsa.john.pub >> ~/.ssh/authorized_keys
$ cat /tmp/id_rsa.josie.pub >> ~/.ssh/authorized_keys
$ cat /tmp/id_rsa.jessica.pub >> ~/.ssh/authorized_keys
----

এখন, `--bare` অপশন দিয়ে `git init` চালিয়ে তাদের জন্য একটা খালি রিপোজিটরি সেটআপ করতে পারেন, যেটা ওয়ার্কিং ডিরেক্টরি ছাড়াই রিপোজিটরিটা initialize করে:(((git commands, init, bare)))

[source,console]
----
$ cd /srv/git
$ mkdir project.git
$ cd project.git
$ git init --bare
Initialized empty Git repository in /srv/git/project.git/
----

তারপর, John, Josie বা Jessica তাদের প্রজেক্টের প্রথম ভার্শন সেই রিপোজিটরিতে পুশ করতে পারেন, এটাকে একটা রিমোট হিসেবে যোগ করে আর একটা ব্রাঞ্চ পুশ করে দিয়ে।
মনে রাখবেন, কোনো নতুন প্রজেক্ট যোগ করতে চাইলে প্রতিবার কাউকে না কাউকে মেশিনে shell করে ঢুকে একটা Bare রিপোজিটরি তৈরি করতে হবে।
আপনি যে সার্ভারে `git` ইউজার আর রিপোজিটরিটা সেটআপ করেছেন, তার hostname হিসেবে `gitserver` ব্যবহার করা যাক।
আপনি যদি এটা internally চালান, আর `gitserver`-এর জন্য সেই সার্ভারকে পয়েন্ট করে DNS সেটআপ করে থাকেন, তাহলে নিচের কমান্ডগুলো প্রায় হুবহু এভাবেই ব্যবহার করতে পারেন (ধরে নিচ্ছি `myproject` একটা বিদ্যমান প্রজেক্ট যাতে ফাইল আছে):

[source,console]
----
# on John's computer
$ cd myproject
$ git init
$ git add .
$ git commit -m 'Initial commit'
$ git remote add origin git@gitserver:/srv/git/project.git
$ git push origin master
----

এই পর্যায়ে, অন্যরাও ঠিক একই সহজে এটা ক্লোন করে নামাতে পারবেন আর পরিবর্তন পুশ করে ফেরত দিতে পারবেন:

[source,console]
----
$ git clone git@gitserver:/srv/git/project.git
$ cd project
$ vim README
$ git commit -am 'Fix for README file'
$ git push origin master
----

এই পদ্ধতিতে, হাতে গোনা কয়েকজন ডেভেলপারের জন্য দ্রুত একটা read/write Git সার্ভার চালু করে ফেলতে পারবেন।

খেয়াল করুন, এখন এই সব ইউজারই সার্ভারে লগ-ইন করে `git` ইউজার হিসেবে একটা shell-ও পেতে পারেন।
এটা আটকাতে চাইলে, আপনাকে `/etc/passwd` ফাইলে shell-টা বদলে অন্য কিছু করতে হবে।

Git-এর সাথে আসা `git-shell` নামের একটা সীমিত shell টুল দিয়ে আপনি সহজেই `git` ইউজার অ্যাকাউন্টটাকে শুধু Git-related কাজেই সীমাবদ্ধ রাখতে পারেন।
`git` ইউজার অ্যাকাউন্টের লগ-ইন shell হিসেবে এটা সেট করলে, সেই অ্যাকাউন্টের আর আপনার সার্ভারে সাধারণ shell access থাকবে না।
এটা ব্যবহার করতে, সেই অ্যাকাউন্টের লগ-ইন shell হিসেবে `bash` বা `csh`-এর বদলে `git-shell` উল্লেখ করুন।
এটা করার জন্য, `git-shell` কমান্ডের পুরো pathname আগে `/etc/shells`-এ যোগ করতে হবে, যদি এটা আগে থেকে সেখানে না থাকে:

[source,console]
----
$ cat /etc/shells   # see if git-shell is already in there. If not...
$ which git-shell   # make sure git-shell is installed on your system.
$ sudo -e /etc/shells  # and add the path to git-shell from last command
----

এখন `chsh <username> -s <shell>` দিয়ে কোনো ইউজারের shell এডিট করতে পারেন:

[source,console]
----
$ sudo chsh git -s $(which git-shell)
----

এখন, `git` ইউজার এখনও SSH connection ব্যবহার করে Git রিপোজিটরি পুশ আর পুল করতে পারবেন, কিন্তু মেশিনে shell করে ঢুকতে পারবেন না।
চেষ্টা করলে, এরকম একটা login rejection দেখবেন:

[source,console]
----
$ ssh git@gitserver
fatal: Interactive git shell is not enabled.
hint: ~/git-shell-commands should exist and have read and execute access.
Connection to gitserver closed.
----

এই পর্যায়ে, ইউজাররা তখনও SSH port forwarding ব্যবহার করে git সার্ভার যত হোস্টে পৌঁছাতে পারে সেসব হোস্টে access করতে পারবেন।
এটা আটকাতে চাইলে, `authorized_keys` ফাইল এডিট করে, যেসব key সীমাবদ্ধ করতে চান তার প্রতিটার শুরুতে নিচের অপশনগুলো prepend করতে পারেন:

[source,console]
----
no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty
----

ফলাফলটা দেখতে এরকম হওয়া উচিত:

[source,console]
----
$ cat ~/.ssh/authorized_keys
no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty ssh-rsa

ICUvax2T9va5 gsg-keypair

no-port-forwarding,no-X11-forwarding,no-agent-forwarding,no-pty ssh-rsa
AAAAB3NzaC1yc2EAAAADAQABAAABAQDEwENNMomTboYI+LJieaAY16qiXiH3wuvENhBG...
----

এখন Git network কমান্ডগুলো ঠিকঠাক কাজ করবে, তবে ইউজাররা shell পাবেন না।
আউটপুটে যেমন বলা আছে, `git` ইউজারের হোম ডিরেক্টরিতে একটা ডিরেক্টরি সেটআপ করে `git-shell` কমান্ডটা কিছুটা customize-ও করতে পারেন।
যেমন, সার্ভার কোন Git কমান্ড গ্রহণ করবে সেটা সীমাবদ্ধ করতে পারেন, বা এভাবে SSH করে ঢুকতে চাইলে ইউজাররা যে মেসেজ দেখেন সেটা customize করতে পারেন।
Shell customize করা নিয়ে আরও তথ্যের জন্য `git help shell` চালান।(((git commands, help)))
